{
  "name": "@vexa/widgets-context1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vexa-cli dev --config ./config/main.ts",
    "publish": "vexa-cli publish",
    "serve": "yarn run tar && yarn run server",
    "tar": "tar -C dist -czvf dist.tgz ./ && cp dist.tgz dist",
    "server": "http-server ./dist -p 8081",
    "lint": "eslint ./ --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "yarn lint --fix"
  },
  "dependencies": {
    "moment": "^2.29.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.2",
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "@types/webpack": "^5.28.0",
    "@vexa/cli": "^1.0.3",
    "dotenv": "^16.3.1",
    "typescript": "^5.0.3"
  }
}
