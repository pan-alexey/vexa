{
  "name": "@vexa/widget-test",
  "version": "1.0.0-dev",
  "private": true,
  "scripts": {
    "dev": "vexa-cli dev --config ./config/main.ts",
    "build": "vexa-cli build",
    "build:default": "vexa-cli build",
    "build:help": "vexa-cli build -h && vexa-cli help build",
    "development": "vexa-cli dev",
    "publish": "vexa-cli publish",
    "help": "vexa-cli help",
    "test:jest": "echo 'jest test with type check'",
    "lint": "eslint ./ --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "yarn lint --fix"
  },
  "devDependencies": {
    "@vexa/cli": "^1.0.0",
    "@types/node": "^18.15.2",
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "@types/webpack": "^5.28.0",
    "typescript": "^5.0.3",
    "node-fetch":"^3.3.1"
  },
  "dependencies": {
    "react": "^18.2.0"
  }
}
